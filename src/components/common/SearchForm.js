import React, { Component } from 'react'
import { From, Input, Button, Col } from 'react-bootstrap'

export default SearchForm extends Component {
  render () {
    return (
      <Col sm={6} smOffset={3} xs={12}>
      <Form inline>
      </Form>
      </Col>
    )
  }
}